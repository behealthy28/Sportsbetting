# Runtime cache and user data (auto-created, not for version control)
data/cache/
data/elo/
data/trades.json
data/watchlist.json
# data/models/ holds the OPTIONAL experimental ML ensemble. It is not shipped:
# on international data Elo+Dixon-Coles beats it, so the model isn't committed.
data/models/
# Note: data/historical/ (real international dataset + recent Elo ratings) IS
# committed so predictions work out of the box. Rebuild with:
#   python -m src.data.build_dataset && python -m src.models.train
# Add the experimental ML ensemble with: python -m src.models.train --ml

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Environment
.env
*.env.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# Test artifacts
.pytest_cache/
.coverage
htmlcov/
__pycache__/
